#Basic animation with processing

void setup()
{
  size(500,400);
}

void draw()
{
  background(0); 
 //set to black,grayscale spectrum
 
  
  stroke(0, 0, 0);  //black outlinr
  fill(60, 220, 90); //green
  circle(mouseX,mouseY,100);
  
}
  
